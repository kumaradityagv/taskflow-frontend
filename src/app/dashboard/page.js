import DashboardView from "@/components/DashboardView";

export const metadata = {
  title: "Dashboard — Taskflow",
  description: "Your Taskflow workspace and board.",
};

export default function DashboardPage() {
  return <DashboardView />;
}
